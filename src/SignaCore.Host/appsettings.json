{
  "//": [
    "Application configuration lives in the business database (system_settings) and is managed",
    "through first-run setup and the authenticated administration pages. What remains here is",
    "deployment/process concerns that cannot come from the database: logging pipelines, the",
    "listening port, and where the writable persistent bootstrap file is expected.",
    "The database connection and the external root key come from config/signacore.bootstrap.json."
  ],
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Warning"
    },
    "Console": {
      "FormatterName": "simple",
      "FormatterOptions": {
        "IncludeScopes": true,
        "TimestampFormat": "yyyy-MM-dd HH:mm:ss.fff "
      }
    }
  },
  "Serilog": {
    "Using": [
      "Serilog.Sinks.Console",
      "Serilog.Sinks.Grafana.Loki"
    ],
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning",
        "Microsoft.EntityFrameworkCore": "Warning"
      }
    },
    "WriteTo": [
      {
        "Name": "Console"
      },
      {
        "Name": "GrafanaLoki",
        "//": "Placeholder only. The effective address comes from the Loki:Uri setting; the sink constructor rejects a null uri, so a syntactically valid fallback has to exist here.",
        "Args": {
          "uri": "http://localhost:3100",
          "labels": [
            {
              "key": "service",
              "value": "SignaCore"
            }
          ]
        }
      }
    ]
  },
  "AllowedHosts": "*",
  "Endpoints": {
    "Http": 5002
  },
  "BootstrapApps": {
    "FilePath": "/app/data/bootstrap-apps.json"
  }
}
