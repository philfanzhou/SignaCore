{
  "//": [
    "Application configuration lives in the shared service_settings aggregate of the business",
    "database and is managed through first-run setup and the authenticated administration pages.",
    "What remains here is",
    "deployment/process concerns that cannot come from the database: logging category levels, the",
    "listening port, and where the writable persistent bootstrap file is expected.",
    "The database connection and the external root key come from config/signacore.bootstrap.json."
  ],
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Endpoints": {
    "Http": 5002
  },
  "BootstrapApps": {
    "FilePath": "/app/data/bootstrap-apps.json"
  }
}
